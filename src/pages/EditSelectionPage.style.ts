import styled from 'styled-components';

export const EditSelectionPageWrapper = styled.div`
  display: grid;
  background-color: ${(p) => p.theme.backgroundColor};
  grid-template-columns: 1fr 1fr;
  padding: 1rem;
`;
